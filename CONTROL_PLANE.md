# BehaviorTune Control Plane

## Status

The dual-state Notion↔GitHub runtime experiment is **DEFERRED** and is not part of active V1/V1.1 execution.

## Active operating mechanism

- Canonical decision state: Notion page `07 — BehaviorTune V1`.
- Executor evidence: project-local git history, artifacts, checksums, tests, manifests, and runtime files.
- Remote GitHub: publication/reproducibility target only; not required for runtime authorization.
- Controller: owns WHY / WHAT / admissibility / next transition.
- Executor: owns HOW inside one explicitly authorized bounded transition.
- Human: receives one compact controller handoff and may return `DONE` after executor writeback.

Do not block active execution because this GitHub repository or mirror is unavailable.

## Current transition

`V1.1-BENCHMARK-REPAIR-DESIGN` / READY — design + deterministic validation only.

The historical dual-state files are retained only as an audit trail.
